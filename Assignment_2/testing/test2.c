
struct hello{
    int x;
    int y;
};
enum MyEnum {
    Y,  // implicitly 0
    Z   // implicitly 1
};

enum Day {
    Monday,
     Tuesday, 
     Wednesday} z;

enum MyEnum x = Y;  // Valid: x is a variable of type enum MyEnum initialized with the enumerator Y
enum Day G = Monday;

int main(){
// typedef int myint;


struct hello y;
}
